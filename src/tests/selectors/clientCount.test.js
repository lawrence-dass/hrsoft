// import ClientCount from '../../selectors/clientCount';
// import Clients from '../fixtures/clients';

// test('should return 0 in case of no client record', () => {
//   const res = ClientCount([]);
//   expect(res).toBe(0);
// });

// test('should correctly show client count', () => {
//   const res = ClientCount([Clients[0]]);
//   expect(res).toBe(1);
// });

// test('should correctly add number of client count', () => {
//   const res = ClientCount(Clients);
//   expect(res).toBe(3);
// });
